# 0023 Phase 4: Rollup adapter

Status: done
Priority: low
Owner: unassigned
Agent: claude-sonnet
Area: adapters
Depends on: 0009, 0010, 0011

## Context
REQUIREMENTS.md §12.3, §21 Phase 4: `@mithril-inspector/rollup` — a thin adapter over the shared `transformMithrilModule` (0009), runtime (0010) and server handler (0011). No duplicated transform or editor-launch logic (§4).

## Acceptance Criteria
- Rollup plugin supports AST transformation, runtime import resolution, watch mode, and source maps (§12.3).
- Editor launching documented and supported via one of: compatible dev-server integration, a separately started inspector server (from `@mithril-inspector/server`), or a configured endpoint URL (§12.3).
- Dev-only guard equivalent to §2.1; production output contains no inspector code.
- Unit tests: transform wiring, virtual/runtime module resolution, map pass-through; a minimal fixture build in `tests/integration/`.

## Implementation Notes
- Vite's plugin (0013) is Rollup-compatible in parts — extract shared adapter utilities rather than copy them, but do not make core packages import Vite (ADR-004).
- Standalone inspector server: expose a small `startInspectorServer(options)` in the server package if not already present from 0011.

## Agent Notes
- 2026-07-15: task created from REQUIREMENTS.md conversion; no work started.
- 2026-07-20 claude-sonnet: Implemented via TDD, in dependency order:
  - **`@mithril-inspector/server`**: added `startInspectorServer(options)` (`start-server.ts`) — a small `node:http` wrapper around the existing `createInspectorMiddleware`, binding to `127.0.0.1` by default (never network-reachable) with no CORS headers (deliberate — same-origin only, matching §10.2's security posture). This is the "separately started inspector server" path §12.3 requires. 6 new tests in `start-server.test.ts` (ephemeral port/url, success, security pipeline still enforced, 404 fallback, explicit host/port, `close()`), verified via `vitest run src/start-server.test.ts` = 6/6.
  - **New `@mithril-inspector/adapter-kit` package**: per the Implementation Notes ("extract shared adapter utilities rather than copy them"), *moved* (not duplicated) `ids.ts`, `virtual-modules.ts`, `module-filter.ts`, `options.ts` and their tests out of `packages/vite` into this new bundler-neutral package (no Vite or Rollup import — ADR-004), so both `vite` and `rollup` now import the same `resolveInspectorOptions`/virtual-module/module-filter logic instead of each having their own copy. Split the HMR wire-protocol constant (`HMR_INVALIDATE_EVENT`/`HmrInvalidatePayload`) out of vite's `hmr.ts` into adapter-kit's new `hmr-protocol.ts` (virtual-modules.ts needed it for the generated bootstrap code; the Vite-*specific* `ModuleIdRegistry`/`normalizeFile` bookkeeping stayed in `packages/vite/src/hmr.ts`, now re-exporting the constant from adapter-kit). Extended `module-filter.ts`'s self-package exclusion regex to also cover `rollup`/`adapter-kit`. `@mithril-inspector/vite`'s own public API (`index.ts`) is unchanged — it re-exports the same names, now sourced from adapter-kit. 32 tests in adapter-kit (`module-filter` 5 incl. 1 new self-exclusion case, `options` 16, `virtual-modules` 9, `index` 2 new), verified via `vitest run` in the package = 32/32. Re-ran vite's full suite after the extraction (34/34) plus the whole workspace (`pnpm -r build`/`typecheck`/`test`, including the real Puppeteer `tests/browser` suite against the rebuilt `@mithril-inspector/vite`) — zero regressions.
  - **`@mithril-inspector/rollup`**: `mithrilInspector(options?, env?): Plugin` — a single Rollup plugin (Rollup has no HTML-transform hook to split off, unlike Vite's two-plugin array). `resolveId`/`load` serve the virtual runtime/overlay modules via adapter-kit; `transform` is registered as `{ order: "pre", handler }` (Rollup's per-hook ordering, the equivalent of Vite's `enforce: "pre"`) and calls the same `transformMithrilModule`. Dev-only guard (§2.1 analog): active only when `resolved.enabled` **and** (`this.meta.watchMode` — Rollup's equivalent of Vite's `command === "serve"`, true only under `--watch`/`rollup.watch()` — **or** `includeInProduction`), so a plain one-off `rollup.rollup()` build emits no inspector code by default. No module-id registry / HMR dispatch needed: watch-mode rebuilds re-run `transform` naturally, and the shared transform's own content-hash-keyed cache (§17, from 0009) already invalidates itself on a changed file. 14 unit tests in `plugin.test.ts`/`index.test.ts` (dev-only gate in/out of watch mode, `includeInProduction` override, full disable, virtual module resolution, transform wiring incl. map pass-through, module-filter skip paths), verified via `vitest run` = 14/14.
  - **Editor launching** (§12.3, AC: "documented and supported via one of..."): deliberately *not* auto-wired into the plugin (Rollup has no dev-server hook to hang it on, and starting an HTTP server as a side effect of a bundler plugin has its own lifecycle/port-conflict hazards). Documented all three permitted patterns concretely in `packages/rollup/README.md`: (1) mount `createInspectorMiddleware` in an existing dev server, (2) run the new `startInspectorServer` as a companion process, (3) proxy to it from whatever serves the Rollup output — necessary because the server binds `127.0.0.1`-only with no CORS, so a cross-origin page can't call it directly by design.
  - **Integration test** (`tests/integration/`, new workspace member registered in `pnpm-workspace.yaml`/`vitest.workspace.ts`): a real `rollup.rollup()` + a real `rollup.watch()` cycle against a Mithril fixture (`mithril`/`@mithril-inspector/*` marked `external` — this proves the plugin's own wiring, not full app bundling). 3 tests, verified via `vitest run` = 3/3: (a) a plain one-off build emits no inspector code by default; (b) `includeInProduction` forces it in; (c) a real `rollup.watch()` cycle activates it by default with no extra options, actually reading the written watch-mode output off disk.
  - **Finding from the integration test** (not a bug, but worth recording): Rollup fully bundles the virtual runtime module into the *same chunk* as the application module (unlike Vite's unbundled dev server), so Rollup's own scope-hoisting/deconfliction collapses the transform's `registerModule as __miRegisterModule` import alias back onto the canonical exported name — the merged output calls `registerModule(...)`/`component(...)`/`source(...)` directly rather than the `__mi*`-prefixed names a Vite dev transform shows. Functionally identical, just spelled differently at the text level; the integration test asserts on the actual (post-hoisting) call shape and documents why in a comment.
  - **Docs/release wiring**: `packages/rollup/README.md` (usage, the watch-mode dev-only guard, all three editor-launch patterns, manual overlay import since Rollup has no HTML injection, options reference pointing at vite's for the shared surface); `packages/adapter-kit/README.md` (what moved here and why, what deliberately stayed Vite-specific); `packages/server/README.md` gained a "Standalone server" section for `startInspectorServer`. `scripts/release.mjs`'s `PACKAGES` list extended to `[..., "overlay", "adapter-kit", "vite", "rollup"]` (adapter-kit before the adapters that depend on it); root `README.md`'s Releasing section and package/file counts updated to match, plus a new "Other bundlers" section pointing at the Rollup adapter. `CLAUDE.md`'s bundler-independence bullet now also names `adapter-kit` and generalizes from "independent of Vite" to "independent of any specific bundler." Set `packages/rollup` and `packages/adapter-kit` to version `0.1.0` (was `0.0.0`/private) to match the other six packages the release script version-syncs; `pnpm release:dry-run` confirms the sync holds across all eight.
  - Verified end-to-end: `pnpm -r build`, `pnpm -r typecheck`, and `pnpm -r test` (root scripts, 20 workspace projects) all clean/green — including the full real-Chromium `tests/browser` suite (27 tests, unaffected) run against the rebuilt `@mithril-inspector/vite`. No known gaps against this task's acceptance criteria.
