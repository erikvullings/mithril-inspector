# 0017 Phase 2: component instance tracking

Status: done
Priority: medium
Owner: unassigned
Agent: claude-opus
Area: runtime
Depends on: 0004, 0010

## Context
REQUIREMENTS.md §21 Phase 2: mounted component-instance IDs and parent-child tracking in the runtime, using the mechanism proven in spike 0004. Produces `ComponentRecord`s (§7.3) with stable IDs (§7.2), ownership scopes (§7.5), and `DomRange` links — the data layer for ancestry (0019) and the full tree (0022).

## Acceptance Criteria
- Stable `ComponentId` per mounted instance across redraws; new instance ⇒ new ID; unmount ⇒ record marked unmounted and cleaned (weakly held, §17).
- `ComponentRecord` fields populated: id, parentId, displayName (raw for now — resolution polish in 0018), source, kind (object/closure/class/function/route-resolver/anonymous), mounted, createdAt/updatedAt/updateCount, domRange, childIds. Attrs/state captured as references only — safe serialization arrives in 0020.
- Vnode ownership per §7.5: enter/leave component scope around wrapped views; nested component vnodes start new scopes; no reliance on global-`m` call-stack interception (ADR-005).
- Nearest-component lookup for a DOM node returns correct instance including fragment roots and keyed reorders.
- `mode: "components"` (§17) activates this tracking; `mode: "source"` skips it; overhead in components mode within the <20% median redraw target on the playground (spot-check, not a benchmark suite).
- Runtime unit tests: parent-child ownership, keyed reorder identity, multiple roots, unmount cleanup, route-resolver components.

## Implementation Notes
- Do not claim correctness until keyed redraws, fragments, multiple roots and unmount cleanup are tested (§25.10).
- `markInspectorHidden` (§14) must exclude a component and its subtree from records.
- Update the 0004 ADR if the production implementation deviates from the spike.

## Agent Notes
- 2026-07-15: task created from REQUIREMENTS.md conversion; no work started.
- 2026-07-17: claude-sonnet implemented the task, entirely in `packages/runtime` (no `protocol`/`transform`/`overlay`/`vite` changes, matching the task's `Depends on: 0004, 0010` and `Area: runtime`).

  **Two conflicts surfaced during investigation, resolved with the user (via `AskUserQuestion`) before implementation:**
  1. §17 defines `mode: "source"` as "element-to-source mapping only," but the already-tagged `v0.1.0-alpha.1` (task 0016) ships nearest-component lookup and basic ancestry as always-on in the *default* `source` mode, verified by existing tests that never set `mode`. Implementing the gate literally would have silently regressed a shipped, tagged release. **Resolved**: kept `source` mode's shipped behavior for object/closure unconditionally; `mode: "components"`/`"full"` gate only the *new* kinds this task adds (class, route-resolver), which had no earlier alpha behavior to preserve. Verified no regression: full `pnpm -r test` stays green, including `tests/browser`'s mode-implicit assertions.
  2. The task's acceptance criteria wants `kind: "route-resolver"` and a "route-resolver components" unit test, but the transform (§6.5) only detects `view`-shaped components — it has no logic to find `{render, onmatch}` object literals in a real `m.route()` table, and this task doesn't depend on 0009 (transform). **Resolved**: runtime-only — `ComponentRegistry` detects and correctly tracks a `{render}`-shaped definition when given one (via `instrument()`/the public `inspectComponent()` API), unit-tested by driving the registry directly (mimicking `mithril/api/router.js`'s real `RouterRoot.view()` → `currentResolver.render(vnode)` call). Real end-to-end tracking through an app's actual route table remains a follow-up needing transform changes — documented, not silently omitted.

  A third finding, not worth a separate question: bare `function Foo() {...}` **declarations** used as closures are permanently unwrappable from the runtime alone (transform discards `__miComponent`'s return for a declaration binding it can't rebind; no runtime-only heuristic distinguishes a declaration from a `const Foo = () => {}` expression — both can carry a `.name`; global-`m` interception is forbidden, ADR-005). Documented explicitly with a passing test proving the current, honest behavior (`components.test.ts` "function-declaration closures" describe block) rather than left as a silent gap.

  **A real spec-vs-implementation correction found while implementing class components**: ADR-103/105 describe the class mechanism as "a prototype facade... applied to a prototype facade" without pinning down *how*. The first implementation attempt — reassigning `Klass.prototype = Object.create(originalPrototype)`, mirroring the object/closure `Object.create(app)` pattern one level up — **throws** in strict mode: a `class` constructor's `.prototype` property is non-writable (unlike a plain `function`'s). Corrected to snapshot the original `view`/hooks into a plain object first, build the same composed wrapper `composeHooks` already produces for object/closure delegating to that snapshot (avoiding self-recursion), and copy its own properties directly onto the *existing* prototype object as own-property overwrites — more invasive than the object/closure path (mutates an object the app still holds a reference to) but the only mechanism available for a declaration-form binding. `instanceof`/`.constructor` against the original class still hold (only specific methods overwritten, not the prototype chain). Documented in both `packages/runtime/README.md` and a new "Production implementation (task 0017)" section appended to `docs/adr/ADR-103-component-instance-tracking.md`.

  **Acceptance criteria, verified:**
  - Stable `ComponentId` per instance across redraws, new instance ⇒ new id, unmount ⇒ marked-unmounted-and-cleaned (weakly held) — unchanged pre-existing behavior for object/closure, extended to class (`components.test.ts` "class components" describe block) and route-resolver (`components.test.ts` "route-resolvers" describe block, "persists across repeated render() calls" documents the resolver-specific caveat: no unmount signal exists, so it stays `mounted: true` for the app's lifetime once allocated).
  - `ComponentRecord` fields populated for every kind, including the new `anonymous` read-time refinement (`toRecord` downgrades a nameless `object`-kind instance, §2.4) — `components.test.ts` "reports kind: anonymous...".
  - Vnode ownership scopes (enter/leave around wrapped views, §7.5) — unchanged mechanism, reused as-is for class (prototype-facade `view`) and route-resolver (`render`); no global-`m` interception anywhere (verified: no import of Mithril's own module needed to `require`/monkey-patch — `components.ts` has zero Mithril import).
  - Nearest-component lookup correct for fragment roots (pre-existing, unchanged) and keyed reorders — new multi-op test (`components.test.ts` "keyed reorder identity", one redraw that simultaneously removes an item, inserts a new one, and moves another, not just a pairwise swap) confirms identity follows the state object, not screen position.
  - `mode: "components"` activates class/route-resolver tracking, `mode: "source"` leaves them inert — `components.test.ts` "mode gating" + "is inert in mode: source" tests in both new describe blocks; `runtime.test.ts` "wires getMode through to the component registry" confirms the `runtime.ts` → registry wiring end-to-end, not just the registry in isolation.
  - Runtime unit tests: parent-child ownership (existing + new class/route-resolver variants), keyed reorder identity (new), multiple roots (existing, re-verified passing), unmount cleanup (existing, re-verified + new class/route-resolver-specific assertions), route-resolver components (new, 4 tests).
  - Performance spot-check (not a CI gate, per the task's own "spot-check, not a benchmark suite" wording): a throwaway local script (not committed) built a synthetic mixed object/class-component tree under jsdom and compared `mode: "source"` vs `mode: "components"` median redraw time using the built `dist/runtime.js`. **Two data points, and they disagree**: a small tree (4-wide × 3-deep, 64 components, 60 redraws × 3 samples) measured source 127.9ms / components 142.8ms → **11.7% overhead**, under the §17 "<20%" target. A larger tree (8-wide × 4-deep, ≈4096 components, 300 redraws × 5 samples) measured source ≈80.8s / components ≈105.1s → **30% overhead**, over target. Nothing mode-gated runs during instrumentation (that happens once, outside the timed loop) or during the timed loop itself in a way that should scale super-linearly by design, so the likely cause is the wrapped `view`'s registry bookkeeping (`scopeStack` push/pop, `recordOwnedVnodes` tree walk, `eachRangeNode`/`registerRange` per flush) compounding across thousands of live instances, plus growing GC/WeakMap pressure — not confirmed with a profiler. **Correction to an earlier version of this note**, which cited only the favorable 11.7% figure as "under target" before the larger run (started in the background, finished after the task was first marked done) completed. Flagging this honestly as a real, unresolved risk for 0019/0022, which add more per-flush work on top of this same data layer — needs profiling against a real app (§20.2's "two nontrivial Mithril applications," which gates STABLE, not this alpha-adjacent task) rather than another synthetic spot-check before drawing a firm conclusion either way.

  **`markInspectorHidden` subtree exclusion** (Implementation Notes item, previously a no-op stub): filtered at the read boundary (`recordOf`, `componentsSnapshot`, `resolveDomComponent`'s innermost-owner walk) rather than at wrap time, since hiding can be toggled before or after `instrument()` ran for the same definition — `components.test.ts` "excludes a hidden component and its subtree..." and "resolveDomComponent skips a hidden owner...".

  **`childIds` render order** (ADR-103's own flagged follow-up, explicitly called out in this task's Implementation Notes): the flush-time rendered-tree walk (`visitForOwnership`) now threads the current parent through its recursion and rebuilds each parent's `childIds` fresh every flush, replacing the old creation-order accumulation. Found and fixed an incidental self-referential-push bug introduced while making this change (the walk's own root vnode self-matches at the top of the recursion since `parent === root` initially; guarded with `record !== parent`) before it could ship. `components.test.ts` "rebuilds childIds in render order on every flush...".

  **Verified**: 17 new tests total this task — 16 in `packages/runtime/src/components.test.ts` (31 total in the file, up from 15) and 1 in `packages/runtime/src/runtime.test.ts` (15 total, up from 14) — via `vitest run src/components.test.ts src/runtime.test.ts` (46 passed). Full package suite: `vitest run` in `packages/runtime` — 87 passed, 8 files, no regressions. `tsc -p tsconfig.json --noEmit` clean (no new `any`, no unused symbols). Whole-workspace `pnpm -r typecheck` and `pnpm -r build && pnpm -r test` green, including `packages/overlay` and `tests/browser` (regression guard for the mode-gating decision above — the tagged `v0.1.0-alpha.1` behavior is unchanged).

  **Known limitations, documented in `packages/runtime/README.md`** ("Component instance tracking" + "Known Phase 1/2 limitations" sections): function-declaration closures permanently untracked (kind `"function"` exists in the protocol type but the runtime cannot produce it); route-resolver tracking is runtime-only pending a transform change; `mode: "full"` is scaffolding, identical to `"components"` today (attrs/state/diagnostics arrive with 0020's safe serializer).
