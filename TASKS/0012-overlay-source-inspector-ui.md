# 0012 Overlay package: tab, picker, highlight, tooltip

Status: done
Priority: high
Owner: unassigned
Agent: claude-opus
Area: overlay
Depends on: 0008, 0010

## Context
REQUIREMENTS.md §4, §8: `@mithril-inspector/overlay` is the in-page UI, written in Mithril itself, mounted in `<div id="__mithril-inspector-host">` with an (open by default) shadow root. Phase 1 scope (§21): collapsed bottom tab, element picker, highlight rectangles, source tooltip/badge, selected-element details with "Open in editor". Consumes the runtime hook (0010) and posts to the editor endpoint (0011 — integration wired in 0013).

## Acceptance Criteria
- Collapsed tab fixed bottom-right by default (§8.1): configurable position, movable, position/collapsed state persisted in localStorage, light/dark following browser scheme, shadow-root isolated, no global styles, no pointer-event capture outside visible region.
- Host excluded from picking and from runtime tracking (§8.2).
- Picker (§8.4): toggle `Alt+Shift+M`, momentary hold `Alt+Shift`, Enter opens source, Escape cancels; all configurable; no plain Alt+Click default.
- Hover (§8.5): capture-phase pointer listener, `document.elementsFromPoint`, overlay ignored, best mapping resolved, highlight drawn without touching target styles, info badge shows component/element/`file:line`.
- Highlight (§8.6): separate fixed-position rectangles, follows scroll/resize/transforms via `getBoundingClientRect`, rAF-throttled, disappears when picker ends.
- Selection (§8.7): click prevented + propagation stopped by default, pass-through modifier available, highlight frozen, details panel shown, picker exits unless continuous mode.
- Stale nodes (§8.8): "Element no longer mounted", nearest-mounted-ancestor offer, no strong refs.
- UI distinguishes exact vs inferred mappings (§2.4) and degrades per the §2.4 fallback ladder.
- Accessibility (§18): keyboard navigable, semantic controls, focus indicators, WCAG AA contrast, no focus trap collapsed, reduced motion respected, shortcuts remappable/disable-able; picker mode visibly indicated.
- Vitest component tests for picker state machine, highlight geometry, and stale-node flow.

## Implementation Notes
- Panel tabs `[ Inspector ] [ Components ] [ Settings ]` (§8.3) — Components tab may be a placeholder until 0019/0022.
- Overlay errors must never break the host app (§16); surface them in a diagnostics view.
- Idle CPU near zero; one pointer update per animation frame (§17).
- Mithril + mithril-materialized conventions per user preferences where they don't conflict with the shadow-DOM/no-global-styles constraints (materialized CSS may need scoping inside the shadow root — evaluate, don't force).

## Agent Notes
- 2026-07-15: task created from REQUIREMENTS.md conversion; no work started.
- 2026-07-16 claude-opus: Implemented `@mithril-inspector/overlay` via TDD, one module per concern, staying Vite-free (ADR-004) and decoupled from the runtime package — the overlay reads the runtime hook through the global `window.__MITHRIL_INSPECTOR__` and defines its needed subset as `OverlayHook` (a compile-time assertion in `hook.test.ts` asserts the real `InspectorRuntime` still satisfies it, so drift is caught without a build-order dependency). Modules: pure logic — `shortcuts.ts` (parse/match chords + modifier-only holds, disable-able per §18), `persistence.ts` (localStorage collapsed/offset with try/catch degradation, §8.1/§16), `mapping.ts` (§2.4 exact-vs-inferred ladder by `SourceLocation.kind`), `picker.ts` (the picker state machine as a pure reducer — toggle vs. momentary hold, hover, select-continuous/exit, cancel), `highlight.ts` (rect geometry from `getBoundingClientRect` + an rAF frame scheduler that coalesces to one update/frame and never self-reschedules, §8.6/§17), `selection.ts` (stale-node handling via `WeakRef` node + captured `WeakRef` ancestor chain; "no longer mounted" + nearest-mounted-ancestor promotion, §8.8), `diagnostics.ts` (§16 error boundary/ring buffer), `options.ts` (defaults+merge), `element-info.ts` (describeElement + host-excluding `elementsFromPoint`), `editor.ts` (POSTs only `{file,line,column}` to `/__mithril-inspector/open-in-editor`, §15), `drag.ts` (movable tab/panel, §8.1). Integration — `controller.ts` composes them into a pull-based `OverlayViewState` and owns the pointer/click/keydown/keyup handlers (capture-phase pass-through modifier, click suppression, Enter/Escape, hold start/stop), all guarded so a hook failure never throws; `styles.ts` (shadow-scoped, theme-aware light/dark following `prefers-color-scheme` with explicit overrides, WCAG AA colors, `:focus-visible`, reduced-motion, pointer-events isolation); `view.ts` (Mithril `OverlayRoot`: tab, `[Inspector][Components][Settings]` panel with `role=dialog/tablist/tab/tabpanel`, hover badge, highlight layer, picker banner `role=status`, stale notice, Settings diagnostics view, exact/inferred precision badge); `overlay.ts` (`mountInspectorOverlay` — shadow-root host `#__mithril-inspector-host`, `hook.excludeHost`, capture-phase listeners, rAF-throttled scroll/resize refresh, HMR-safe remount, `dispose()`). Evaluated `mithril-materialized` and deliberately did **not** use it — it ships global CSS which conflicts with the shadow-root/no-global-styles constraint; used a self-contained scoped stylesheet instead (noted in README). Verified: 117 tests across 15 files, all green via `pnpm --filter @mithril-inspector/overlay test` (shortcuts 13, persistence 7, mapping 9, picker 12 incl. interleaved hold→toggle→release sequences, highlight 8, selection 8 incl. skip-disconnected-ancestor ordering, diagnostics 5, options 5, hook 3, element-info 6, editor 5, drag 4, controller 17, overlay 14 mount/integration, index 1). Package `tsc -p tsconfig.json --noEmit` clean and declaration build succeeds; `pnpm -r typecheck` and `pnpm -r test` green across the whole workspace (399 tests total; all sibling packages unaffected). Also drove the built `dist` end-to-end in jsdom (mount → startPicker → hover resolves component/element/exact `src/UserCard.ts:17:5` → click selects → details panel + Open-in-editor render with the correct ARIA roles and precision badge). Acceptance criteria mapped: collapsed tab + configurable position + movable + persisted + light/dark + shadow-isolated + no global styles + pointer-events only on visible regions ✔; host excluded from picking & tracking ✔; picker toggle/hold/Enter/Escape all configurable, no Alt+Click default ✔; hover capture-phase + elementsFromPoint + overlay ignored + best mapping + highlight without touching target styles + info badge ✔; separate fixed rects following scroll/resize/transforms, rAF-throttled, disappear on picker end ✔; selection click-prevent+stop + Meta pass-through + freeze + details + exit-unless-continuous ✔; stale-node notice + ancestor offer + WeakRef ✔; exact-vs-inferred UI + §2.4 degrade ✔; accessibility (semantic controls, roles, focus indicators, WCAG AA, reduced-motion, remappable/disable-able shortcuts, visible picker indicator) ✔; Vitest tests for picker state machine / highlight geometry / stale-node flow ✔. Known Phase-1 limitations (see README): Components tab & ancestry show only the nearest component (full tree/ancestry deferred to 0019/0022); highlight margin/padding visualization deferred; real-browser integration is exercised by 0014 (playground) and 0015 (browser tests). Integration wiring into Vite (host injection + editor middleware) is task 0013 as noted.
