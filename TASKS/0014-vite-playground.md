# 0014 Vite playground application

Status: done
Priority: high
Owner: unassigned
Agent: claude-sonnet
Area: playground
Depends on: 0013

## Context
REQUIREMENTS.md §4 (`apps/playground-vite`), §25: "The first concrete deliverable shall be a working Vite playground in which clicking an instrumented Mithril-rendered element opens the exact original TypeScript source line in VS Code." Also serves as the manual test bed for every later milestone (§25.7) and the base for browser-test fixtures (0015).

## Acceptance Criteria
- TypeScript Mithril 2.x app using only `plugins: [mithrilInspector()]` in `vite.config.ts` — no app-code changes (§2.2).
- Exercises the §19.2 fixture scenarios: simple mounted component, routing (`m.route`), nested components, list redraws, keyed reordering, fragment-root component, trusted HTML, SVG, in-app shadow DOM, multiple mount roots, component removal, scrolling content, CSS transforms, dialog/high z-index content.
- Manual walkthrough of §24 works end-to-end: `pnpm dev` → tab visible → picker → hover badge (component, tag, `src/...:line:col`) → click suppressed + selection shown → "Open in editor" lands on the exact original TS line in VS Code.
- `pnpm build` on the playground produces a bundle with zero inspector code (grep for `__MITHRIL_INSPECTOR__`/runtime markers).

## Implementation Notes
- Keep components small and named so display-name resolution (0018) is testable later.
- Include at least one closure, one object, and one class component (§6.5).
- HMR check: edit a component while dev server runs; mappings must survive (0007/0013).

## Agent Notes
- 2026-07-15: task created from REQUIREMENTS.md conversion; no work started.
- 2026-07-17 claude-sonnet: Implemented the real playground (previously only a placeholder README; 0015 had worked around this with its own dedicated fixtures under `tests/browser/fixtures/app/`, per that task's own agent notes).
  - Built `apps/playground-vite` as a routed Mithril 2.x + TypeScript app: `vite.config.ts` is exactly `plugins: [mithrilInspector()]` (§2.2, zero app-code changes). Structural inspiration (routing-service class, layout/nav split, small named pages) taken from the user's own scaffold at https://github.com/erikvullings/mithril-app (installed locally at `../mithril-app`), kept dependency-light (no mithril-materialized/meiosis/translate.js — not needed for an instrumentation testbed).
  - One route per §19.2 scenario not otherwise covered by the app's own structure: `/` (object component, nested), `/list` (closure, list redraws + keyed reordering + component removal), `/fragment` (fragment-root array `view`), `/trusted-html` (`m.trust`), `/svg`, `/shadow-dom` (in-app `attachShadow`, distinct from the overlay's own host), `/dialog` (**class** component driving a native `<dialog>`), `/scroll`, `/transform` (CSS transforms). Routing itself and a second independent `m.mount()` (`StatusWidget`) cover the two non-route scenarios (routing, multiple mount roots). Component-form mix (§6.5) verified: object (8 components), closure (4, all written as `const X = () => {...}` per the runtime's Phase-1 instrumentation constraint — not `function X() {...}`), class (1, `DialogPage`).
  - **Cross-package fix required**: the plugin's virtual modules (`virtual:mithril-inspector/{runtime,overlay}`) import `@mithril-inspector/runtime`/`overlay` as bare specifiers with no resolvable "importer" directory; pnpm's strict `node_modules` can't resolve them as transitive deps of `@mithril-inspector/vite` alone (same root cause 0015 hit with its fixture copy). Fixed by adding `@mithril-inspector/{overlay,protocol,runtime,server,transform,vite}` as direct `devDependencies` of `apps/playground-vite`, matching `tests/browser`'s package.json pattern. Without this, `pnpm dev` fails with `Failed to resolve import "@mithril-inspector/overlay" from " virtual:mithril-inspector/overlay"`.
  - **Verified honestly, against the real running dev server** (not just unit tests), via the `browser-tools` skill (Puppeteer/CDP) against a real `vite` dev process on port 5199:
    - Tab appears (`◇ Mithril Inspect`), collapsed/expanded state persists in `localStorage` (`__mithril-inspector-overlay`, confirming §8.1's "remembers its position and collapsed state").
    - Picker activates (`.mi-picking-banner` shown, `aria-pressed="true"`).
    - Hovering the home route's `Counter` button shows the exact badge: component `Counter`, element `button#counter-btn`, source `src/pages/counter.ts:16:9` — matching the real source line.
    - Clicking while picking: the app's own `onclick` (`count += 1`) is suppressed (`#counter-value` stayed `"0"`), the selection panel populated (`Component: Counter`, `Element: button`, `Source: src/pages/counter.ts:16:9`, `Mapping: Exact element source`), and picking mode exited (assertion 9 + 4 confirmed live).
    - Editor endpoint: a real `fetch` POST to `/__mithril-inspector/open-in-editor` fired automatically (`picker.openOnClick` default). With no editor configured it correctly returned `{"ok":false,"error":{"code":"EDITOR_NOT_AVAILABLE"}}` (§10.3's no-default-guessing behavior — confirmed intentional, not a bug, by reading `packages/server/src/editors.ts`). Re-ran with `EDITOR=code pnpm dev` (this machine has `/opt/homebrew/bin/code` on `PATH`) and got `{"ok":true}` — **VS Code actually launched** at `apps/playground-vite/src/pages/counter.ts:16:9`, the literal §25 acceptance bar ("clicking an instrumented Mithril-rendered element opens the exact original TypeScript source line in VS Code"), not a mocked assertion.
    - All 9 routes render correctly (`/list` keyed shuffle + per-item removal, `/dialog`'s class component opens/closes a real native `<dialog>`, `/shadow-dom` has a genuine attached `shadowRoot`, `/fragment` correctly has no wrapping element). The second `m.mount()` (`#status-widget`) stayed mounted across every route change.
    - HMR: edited `counter.ts` (shifted the button's line 16→17, changed its label) while `pnpm dev` ran. Vite logged `page reload src/pages/counter.ts` (full-reload fallback — Mithril has no HMR plugin, same finding as 0015/ADR-106). After the reload, the new label rendered and the hover badge correctly reported the **new** line (`src/pages/counter.ts:17:9`), confirming mappings survive/update, not just "app didn't crash." The edit was reverted afterward — `counter.ts` is back to its original content.
    - Production build: `pnpm --filter @mithril-inspector/playground-vite build` produces `dist/{index.html, assets/index-*.css, assets/index-*.js}`; grepping the whole `dist/` for `__MITHRIL_INSPECTOR__`, `__miRegisterModule`, `__miSource`, `__miComponent`, `mountInspectorOverlay`, `virtual:mithril-inspector`, `open-in-editor`, `mithril-inspector` — zero matches (§20.1.12).
  - **Environment finding (tooling, not a product bug)**: driving this via CDP against a real, non-headless Chrome window whose tab wasn't OS-visible showed `document.hidden === true` even after `page.bringToFront()`, which pauses `requestAnimationFrame` — so Mithril's `m.redraw()` (rAF-scheduled) and the overlay's own frame-coalesced pointer/highlight scheduling never fired. Diagnosed by first confirming the click's own DOM dispatch worked (a manually-attached probe listener fired) and then forcing `m.redraw.sync()` via a dynamic `import()` of the exact resolved `mithril.js` dep-optimizer URL (confirmed same singleton via `performance.getEntriesByType("resource")`), which immediately produced the expected panel expansion — isolating the stall to rAF-throttling, not application logic. Worked around cleanly for the rest of the walkthrough via `page.evaluateOnNewDocument` shimming `requestAnimationFrame` to `setTimeout(fn, 0)` before each reload.
  - Typecheck: `pnpm --filter @mithril-inspector/playground-vite typecheck` clean. Build: `pnpm --filter @mithril-inspector/playground-vite build` clean (49 modules transformed, ~32 kB / 12.5 kB gzip).
  - Known non-gap: 0015's browser-test suite still uses its own dedicated `tests/browser/fixtures/app/` rather than this playground — that task is marked `done` and wasn't reopened here; nothing in this task's acceptance criteria requires migrating it, though the playground could now serve as a richer fixture source for a future task if desired.
